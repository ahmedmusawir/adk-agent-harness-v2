# Session Log: 2026-03-23

## Project Context

- **Project:** ADK Harness Starter v1
- **Tool:** Claude Code
- **Goal:** TBD — awaiting first task

## Starting State

- **Branch:** main
- **Last Working Feature:** SK-1 (token_calculator), SK-2 (run_receipt), SK-4 (context_cache), SK-INT (receipt callbacks wired into all 3 agents), ghl_mcp_agent locationId bug fixed, usage_report.py CLI — 42/42 tests green as of last session (2026-03-03)
- **Known Issues:** None

## Session Progress

### 11:00 — Config file + venv fix + manual finalized

- Added `evals/eval_config.json` — agent, project, description, default_dataset
- Updated `scripts/run_evals.py` — `load_config()`, `--config`/`--dataset`/`--agent` flags
- Added 2 config unit tests — 54/54 green
- Fixed venv portability issue — all packages were in pyenv, not .venv; rebuilt fresh venv + pip install -r requirements.txt; confirmed eval runs cleanly from venv
- Updated `docs/EVAL_TESTING_MANUAL.txt` — added env setup, config section, thought_signature warning doc, updated run commands
- ADK Harness Starter Kit V1 officially locked

### 10:00 — Eval Testing Manual + session update

- Created `docs/EVAL_TESTING_MANUAL.txt` — human-readable manual covering: file map, how to run, where to find results, how to add cases, grading logic, unit test instructions
- User manually tested eval runner — confirmed working, 3/5 PASS on first run

### 09:00 — SK-6 Eval Runner built and tested

- Created `evals/eval_cases.json` — 5 test cases for product_agent_rico_1
- Created `scripts/run_evals.py` — async CLI runner, drives agent through ADK Runner so receipt_callback fires; deterministic substring grading; saves ASCII report to `evals/reports/`
- Created `tests/test_eval_runner.py` — 10 unit tests (JSON parsing, exec, report gen, file save), all mocked, all green
- Updated `README.md` — eval + usage report sections
- Updated `.gitignore` — added `evals/reports/`
- Baseline: 42 tests. After SK-6: 52/52 green
- ADK Harness Starter Kit V1 officially locked

## Lessons Learned

*(carry-forward from prior sessions)*
- GHL MCP tools require locationId injected into system prompt via f-string
- `product_agent_rico_1` fetch_instructions key ("product_agent") doesn't match folder name — pre-existing inconsistency
- Vertex AI: tools + cached_content CANNOT coexist in generate_content config (API 400) — tools must be baked into CreateCachedContentConfig at cache creation time

## End of Session State

- **Working:** All 54 tests green. SK-6 eval runner complete with config file support. Venv fixed and portable. Starter Kit V1 fully locked.
- **Broken:** Nothing
- **Next Steps:** Architect agent planning (Tony + architect team offline session)

## Files Changed This Session

- `evals/eval_cases.json` — created (5 eval cases)
- `evals/eval_config.json` — created (agent, project, description, default_dataset)
- `scripts/run_evals.py` — created (SK-6 CLI runner + config/dataset/agent flags)
- `scripts/usage_report.py` — updated (saves to reports/usage_reports/)
- `tests/test_eval_runner.py` — created (12 unit tests, all mocked)
- `README.md` — updated (eval + usage report docs)
- `.gitignore` — updated (reports/, evals/reports/)
- `CHANGELOG.md` — updated
- `docs/EVAL_TESTING_MANUAL.txt` — created + updated (env setup, config usage, thought_signature warning)
- `session_2026-03-23.md` — created
