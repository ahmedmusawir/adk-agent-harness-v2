# Session Log: 2026-04-02

## Project Context
- **Project:** ADK Harness v1 — architect_agent composite build
- **Tool:** Claude Code
- **Goal:** TBD — awaiting first task

## Starting State
- **Branch:** dual-system-prompt-1
- **Last Working Feature:** All 4 build steps from MASTER_BRIEF complete — dual instructions, session memory (read/write), skills loader (invoke_skill) — 66/66 tests green as of 2026-03-27
- **Known Issues:** None
- **Remaining from prior sessions:**
  - Eval cases (7 scenarios from MASTER_BRIEF → `architect_agent/tests/eval_cases.json`)
  - Manual smoke test via `adk web`
  - Then: RAG integration (Phase 2)

## Session Progress

### [09:00] — Step 4: Datetime Infrastructure (Parts A & B)
- **Part A:** Added `get_current_datetime()` to `architect_agent/tools.py` — returns ISO 8601 UTC string
- **Part B:** Added `get_timestamp_inject_callback()` to `callbacks/receipt_callback.py` — injects `[SYSTEM_TIMESTAMP: ...]` into system instruction via `llm_request.append_instructions()` on every turn
- Wired both into `architect_agent/agent.py`:
  - `get_current_datetime_tool` added to tools list
  - `before_model_callback` upgraded from single callback to list: `[get_start_time_callback(), get_timestamp_inject_callback()]`
- ADK confirmed: `before_model_callback` natively accepts a list — callbacks run in order, no wrapping needed
- Created `architect_agent/tests/test_datetime_tools.py` with 2 unit tests
- Added `test_timestamp_callback_injects_timestamp` to `tests/test_receipt_callback.py`
- Result: **69/69 tests green**

## Lessons Learned

*(carry-forward)*
- ADK v1.13.0: `google_search` (built-in tool) cannot mix with custom FunctionTools — must wrap in sub-agent via `AgentTool(agent=...)`
- Always assign named variables before putting them in arrays — no inline instantiation
- Patch `architect_agent.tools.list_gcs_files` directly in tests (not `storage.Client`) to avoid mock collision when two patches resolve to the same module object

## End of Session State
- **Working:** TBD
- **Broken:** Nothing
- **Next Steps:** TBD

### [10:30] — Steps 5 & 6: Skill path restructure + Context reader tool
- **Step 5:** Updated `invoke_skill` path logic — `SKILL_INDEX` stays flat, all other skills use `{skill_name}/SKILL.md` subfolder structure. Updated 2 test path assertions.
- **Step 6:** Added `read_context_doc(doc_name)` to `architect_agent/tools.py` — reads `{BASE_FOLDER}/architect_agent/context/{doc_name}.md`. CONTEXT_INDEX works same path. Registered as `read_context_doc_tool`, wired into agent.py.
- Created `architect_agent/tests/test_context_tools.py` with 4 unit tests
- Result: **73/73 tests green**

## Files Changed This Session
- `architect_agent/tools.py` — added `get_current_datetime()` + `get_current_datetime_tool`
- `callbacks/receipt_callback.py` — added `get_timestamp_inject_callback()`
- `architect_agent/agent.py` — added `get_current_datetime_tool` to tools list; `before_model_callback` upgraded to list
- `architect_agent/tests/test_datetime_tools.py` — created (2 unit tests)
- `tests/test_receipt_callback.py` — added `test_timestamp_callback_injects_timestamp`
- `architect_agent/tools.py` — updated `invoke_skill` path logic; added `read_context_doc` + `read_context_doc_tool`
- `architect_agent/agent.py` — added `read_context_doc_tool` to tools list
- `architect_agent/tests/test_skill_tools.py` — updated 2 path assertions for new subfolder structure
- `architect_agent/tests/test_context_tools.py` — created (4 unit tests)
