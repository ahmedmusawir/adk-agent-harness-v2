# Session Log: 2026-03-27

## Project Context
- **Project:** ADK Harness Starter v1
- **Tool:** Claude Code
- **Goal:** TBD — awaiting first task

## Starting State
- **Branch:** dual-system-prompt-1
- **Last Working Feature:** SK-1 (token_calculator), SK-2 (run_receipt), SK-4 (context_cache), SK-INT (receipt callbacks), ghl_mcp_agent locationId fix, usage_report.py CLI, SK-6 eval runner with config file support — 54/54 tests green as of 2026-03-25
- **Known Issues:** None

## Session Progress

### [15:00] — Step 3: invoke_skill tool
- Added `invoke_skill(skill_name)` to `architect_agent/tools.py`
- Loads `{BASE_FOLDER}/globals/skills/{skill_name}.md` from GCS, returns content as string
- Missing file returns clear error message (not exception)
- Registered as `invoke_skill_tool = FunctionTool(func=invoke_skill)`
- Added to `architect_agent/agent.py` tools list
- Created `architect_agent/tests/test_skill_tools.py` with 3 unit tests (all pass)
- Result: **66/66 tests green**

### [14:00] — Step 2: Session Memory Tools (write_session_memory, read_session_memory)
- Created `architect_agent/tools.py` with `write_session_memory` and `read_session_memory` functions
- Both tools read `GCS_BUCKET_NAME`/`GCS_BASE_FOLDER` from env; `agent_name` hardcoded as `architect_agent`
- `write_session_memory`: read-before-write append pattern, timestamped entries, creates file if not exists
- `read_session_memory`: lists sessions/, filters by date window, returns content newest-first
- Added `write_gcs_file()` and `list_gcs_files()` helpers to `utils/gcs_utils.py`
- Wired both tools into `architect_agent/agent.py` alongside `google_search`
- Created `architect_agent/tests/test_session_tools.py` with 5 unit tests (all pass)
- Result: **63/63 tests green**

### [13:30] — Combined Step 0+1: fetch_dual_instructions + architect_agent wired up
- Added `fetch_dual_instructions(agent_name)` to `utils/gcs_utils.py`
- Reads `GCS_BUCKET_NAME` and `GCS_BASE_FOLDER` from env vars (not hardcoded)
- Loads global prompt + agent identity prompt from GCS, concatenates with delimiter
- Falls back to identity-only if global fails; returns error string if identity fails
- Updated `architect_agent/agent.py` to use `fetch_dual_instructions` instead of `fetch_instructions`
- Added `GCS_BUCKET_NAME` and `GCS_BASE_FOLDER` to `architect_agent/.env`
- Created `architect_agent/tests/test_gcs_utils.py` with 4 unit tests (all pass)
- Result: **58/58 tests green**

## Lessons Learned

*(carry-forward from prior sessions)*
- GHL MCP tools require locationId injected into system prompt via f-string
- `product_agent_rico_1` fetch_instructions key ("product_agent") doesn't match folder name — pre-existing inconsistency
- Vertex AI: tools + cached_content CANNOT coexist in generate_content config (API 400) — tools must be baked into CreateCachedContentConfig at cache creation time

## End of Session State
- **Working:** 66/66 tests green. architect_agent is a fully wired composite agent:
  - Dual instruction loading (global + identity prompts from GCS)
  - Session memory read/write (GCS-backed, dated session files)
  - Skills loader (invoke_skill loads any skill file from globals/skills/)
  - Search via search_specialist sub-agent (AgentTool pattern, ADK v1.13.0 workaround)
- **Broken:** Nothing
- **Next Steps:**
  - More tests needed (eval cases from MASTER_BRIEF — 7 eval scenarios)
  - Manual smoke test: adk web → dual prompt, session write/read, skill invocation, out-of-scope pushback
  - Then: RAG integration (Layer 3 — Google Managed RAG, playbook ingestion)

## Files Changed This Session
- `utils/gcs_utils.py` — added `fetch_dual_instructions()` and `DUAL_INSTRUCTION_DELIMITER`; added `import os`
- `architect_agent/agent.py` — swapped `fetch_instructions` → `fetch_dual_instructions`
- `architect_agent/.env` — added `GCS_BUCKET_NAME` and `GCS_BASE_FOLDER`
- `architect_agent/tests/__init__.py` — created (empty package file)
- `architect_agent/tests/test_gcs_utils.py` — created (4 unit tests for dual loader)
- `utils/gcs_utils.py` — added `write_gcs_file()` and `list_gcs_files()` helpers
- `architect_agent/tools.py` — created (write_session_memory, read_session_memory + FunctionTool instances)
- `architect_agent/agent.py` — added write_session_memory_tool and read_session_memory_tool to tools list
- `architect_agent/tests/test_session_tools.py` — created (5 unit tests for session tools)
